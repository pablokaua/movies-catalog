<%= form_with model: @director do |f|%>
  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :nationality, 'Nacionalidade' %>
  <%= f.text_field :nationality %>
  <br>
  <%= f.label :bith_date, 'Data de Nascimento' %>
  <%= f.date_field :bith_date %>
  <br>
  <%= f.label :favorite_genre_id, 'Gênero Favorito' %>
  <%= f.collection_select :favorite_genre_id, MovieGenre.all, :id, :name%>
  <br>
  <%= f.submit 'Salvar' %>
<%end%>